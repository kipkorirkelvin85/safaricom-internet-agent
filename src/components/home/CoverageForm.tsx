"use client";

import { AlertCircle, CheckCircle2, MessageCircle, Send } from "lucide-react";
import { FormEvent, useState } from "react";
import { phone } from "@/lib/site";
import { whatsappLink } from "@/lib/whatsapp";

type FormValues = { name: string; phone: string; area: string; connection: string };
const initial: FormValues = { name: "", phone: "", area: "", connection: "" };

export function CoverageForm() { const [values, setValues] = useState(initial); const [error, setError] = useState(""); const [submitted, setSubmitted] = useState(false);
  function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (Object.values(values).some((value) => !value.trim())) { setError("Please complete every field so Jackie can help with your request."); setSubmitted(false); return; } setError(""); setSubmitted(true); const message = `Hello Jackie, I would like to check home internet coverage.\n\nName: ${values.name}\nPhone: ${values.phone}\nArea: ${values.area}\nPreferred connection: ${values.connection}`; window.open(whatsappLink(message), "_blank", "noopener,noreferrer"); }
  return <section id="coverage" className="scroll-mt-20 bg-[#111122] py-20 lg:py-28"><div className="mx-auto grid max-w-7xl gap-12 px-5 lg:grid-cols-[0.8fr_1.2fr] lg:items-center lg:px-8"><div><p className="mb-3 text-xs font-extrabold uppercase tracking-[0.18em] text-[#8ce0af]">Coverage request</p><h2 className="text-3xl font-extrabold tracking-[-0.04em] text-white sm:text-4xl">Check coverage in your area</h2><p className="mt-5 max-w-md leading-7 text-white/65">Tell us where you are and Jackie can help confirm the available Fibre or 5G options.</p><div className="mt-8 flex items-center gap-3 border-t border-white/10 pt-6"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#00a651] text-white"><MessageCircle size={19} /></span><p className="text-sm text-white/70">Prefer WhatsApp? <a href={whatsappLink("Hello Jackie, I would like help checking home internet coverage.")} target="_blank" rel="noreferrer" className="font-bold text-[#8ce0af] hover:text-white">Chat with Jackie — {phone}</a></p></div></div><form onSubmit={submit} noValidate className="rounded-2xl bg-white p-6 shadow-2xl sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><Field label="Full Name" id="full-name" value={values.name} onChange={(value) => setValues({ ...values, name: value })} placeholder="e.g. Kelvin Otieno" /><Field label="Phone Number" id="phone" type="tel" value={values.phone} onChange={(value) => setValues({ ...values, phone: value })} placeholder="e.g. 07XXXXXXXX" /><Field label="Estate / Area" id="area" value={values.area} onChange={(value) => setValues({ ...values, area: value })} placeholder="e.g. Kilimani" /><label className="grid gap-2 text-sm font-bold text-[#344054]" htmlFor="connection">Preferred Connection<select id="connection" value={values.connection} onChange={(event) => setValues({ ...values, connection: event.target.value })} className="min-h-12 rounded-xl border border-[#d0d5dd] bg-white px-3 font-normal text-[#344054] outline-none transition focus:border-[#00a651] focus:ring-4 focus:ring-[#00a651]/10"><option value="">Select an option</option><option>Fibre</option><option>5G</option><option>Not Sure</option></select></label></div>{error && <p role="alert" className="mt-5 flex items-center gap-2 text-sm font-semibold text-[#b42318]"><AlertCircle size={17} />{error}</p>}{submitted && <p role="status" className="mt-5 flex items-center gap-2 text-sm font-semibold text-[#087a38]"><CheckCircle2 size={17} />Your WhatsApp request is ready to send.</p>}<button type="submit" className="mt-7 inline-flex min-h-13 w-full items-center justify-center gap-2 rounded-xl bg-[#00a651] text-sm font-extrabold text-white transition hover:bg-[#087a38] focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-[#00a651]/25">Check Coverage <Send size={17} /></button><p className="mt-4 text-center text-xs leading-5 text-[#98a2b3]">This is a coverage request. Availability is confirmed with Jackie and is not checked automatically.</p></form></div></section>; }

function Field({ label, id, value, onChange, placeholder, type = "text" }: { label: string; id: string; value: string; onChange: (value: string) => void; placeholder: string; type?: string }) { return <label className="grid gap-2 text-sm font-bold text-[#344054]" htmlFor={id}>{label}<input id={id} required type={type} value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} className="min-h-12 rounded-xl border border-[#d0d5dd] px-3 font-normal text-[#344054] outline-none transition placeholder:text-[#98a2b3] focus:border-[#00a651] focus:ring-4 focus:ring-[#00a651]/10" /></label>; }

